# 服务监听
PORT=8080
HOST=0.0.0.0
NODE_ENV=development
API_PREFIX=/api

# 对外访问根 URL，用于 definition 中 chatPluginConfig.pluginUrl；未设置时回退 OPERATORS_BASE_URL / PROTOCOL+HOST+PORT
PUBLIC_BASE_URL=http://localhost:8080

OPERATORS_BASE_URL=http://localhost:8080
PROTOCOL=http

CORS_ORIGIN=*
